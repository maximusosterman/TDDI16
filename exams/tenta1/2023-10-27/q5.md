```C++

std::unordered_set<int> laughs_from_crowd {}; // A set to remove duplicates

std::vector<std::vector<int>> clip {};

std:unordered_map<int, int> data {}; // seat number mapped to number of laughs for it

for (auto& clip : clips) {
    for (auto& seat : laughs_from_crowd) {
        data[seat] += 1;
    }
}

```
