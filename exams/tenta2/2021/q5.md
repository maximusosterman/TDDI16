```C++


double get_reading() {
    //psuedo
    return reading...
}

int main() {

    // K måste vara ett positivt nummer i och med att tid inte kan vara < 0
    unsigned int k {};
    cin >> k;

    // Det är bra att använda en statisk längd på data strukturen, för att vi vet längden - k
    array<k, std::pair<double, double>> averages {}; // Pair håller inputen i watt och parar den mot vad det gav för average.

    double sum = 0; // Total

    // Börjar på 1 för att slippa division med 0
    for (int seconds = 1; seconds <= k; seconds++) {
        auto reading = get_reading();

        // Lägger till inputen på summan
        sum += reading;
        double curr_avrg = sum / seconds; // Räknar ut nuvarande medel

        // Sparar i listan och sparar inputen mot vad den gav för average
        averages.push_back(
            {reading, average}
        );
    }

    for (auto& w : averages) {
        cout << "Second " << k << "|" << "Input: " << w.first << "\nAvrg: " << w.second << endl;
    }



    return 0;
}

```

d) För att lägga till i *ett* värde så har det O(1)
e) Att ta emont n mätvärden är O(n) och att skriva ut k medelvärden har O(k). O(n) + O(k)
f) Minnet för detta är statiskt och har O(k)
3p
